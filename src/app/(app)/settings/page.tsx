import { ModulePlaceholder } from "@/components/layout/module-placeholder";

export default function SettingsPage() {
  return <ModulePlaceholder description="当前设置只服务个人账号，后续可维护显示名称、时区和基础偏好。" eyebrow="SETTINGS" phase="基础占位" title="设置" />;
}
