# Reviewed templates

Optional OpenCV templates belong here after being cropped from the exact fixed
environment and manually reviewed. No template is included yet because a
template captured from an unrelated contact or state would create a false
confidence signal.

Templates must never include or target the final Send/Confirm button.
